image: gradle:jdk16

stages:
  - test
  - build

test:
  stage: test
  script:
    - ./gradlew test
  artifacts:
    reports:
      junit: build/test-results/test/**/TEST-*.xml

build:
  stage: build
  script:
    - ./gradlew assemble
